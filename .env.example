# Modified by Ayyaz Zafar, 2026-09-25: added TYPESAFE_API_KEY option.
# Option A: your own TypeSafe key from https://console.typesafe.ai/keys (calls Jev directly).
# If set, this takes priority over AI_GATEWAY_API_KEY.
TYPESAFE_API_KEY=

# Option B: a Vercel AI Gateway key (not an OpenAI key).
# This stays on the backend. Never use a VITE_ prefix for secrets.
AI_GATEWAY_API_KEY=

# Required on Vercel. Optional for a loopback-only local backend.
# Share this app-specific token only with people allowed to use your backend.
# Enter the same token in Needle extension settings / the web app's How it works dialog.
NEEDLE_ACCESS_TOKEN=

# Optional local port (default 4199).
PORT=4199

# Alternative to AI_GATEWAY_API_KEY: explicit local credential file path.
# AI_GATEWAY_KEY_FILE=/absolute/path/to/key
